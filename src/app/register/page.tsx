import RegisterPage from "@/components/auth/RegisterPage";

export const metadata = {
  title: "Create Account — ZeeFood Premium",
  description: "Join ZeeFood for a premium food delivery experience.",
};

export default function RegisterRoute() {
  return <RegisterPage />;
}
